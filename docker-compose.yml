services:
  docs:
    build:
      context: .
    ports:
      - "127.0.0.1:8088:8080"
    read_only: true
    tmpfs:
      - /var/cache/nginx
      - /var/run
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
